# Напишите программу, которая принимает на вход координаты точки (X и Y), 
# причём X ≠ 0 и Y ≠ 0 и выдаёт номер четверти плоскости, 
# в которой находится эта точка (или на какой оси она находится).

x = int(input("Введите координат X, который не = 0:"))
y = int(input("Введите координат У, который не = 0:"))
if x > 0 and y > 0:
    print("Точка раположена в первой четверти.")
if x < 0 and y > 0:
    print("Точка расположена в второй четверти.")
if x < 0 and y < 0:
    print("Точка расположена в третьей четверти.")
if x > 0 and y < 0:
    print("Точка расположена в четвертой четверти.")